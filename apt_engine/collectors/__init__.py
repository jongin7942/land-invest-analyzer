"""국토부 아파트 매매/전월세 실거래가, K-apt 단지정보, 공시가격 수집기.
기존 collectors/land_trade.py 의 FIELD_CANDIDATES(필드 후보 배열) 패턴을 그대로 쓴다 —
API 필드명이 개편돼도 견디게 하기 위해서다.

여기서 가장 어려운 건 수집이 아니라 matcher.py — 실거래 API 의 단지명 문자열을
K-apt 단지코드에 붙이는 일이다. 표기 흔들림('e편한세상'/'이편한세상'), 동명 단지,
블록 분리 단지 때문에 문자열 매칭만으로는 안 되고, 법정동코드 + 정규화 단지명 +
사용승인연도 + 전용면적 집합의 다중키로 붙이고 match_confidence 를 남겨야 한다.

담당: PHASE 1. 아직 비어 있다.
"""

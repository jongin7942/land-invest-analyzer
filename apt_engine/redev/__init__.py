"""재건축·재개발 사업성 (PHASE 6).

계획서에서 "최상 난이도"로 분류한 구간이다. 어려운 건 산식이 아니라 **입력**이다.
대지면적·정비계획 용적률·평당 공사비·종전자산평가액은 공공 API 로 나오지 않는다.

그래서 두 단계로 나눈다(2단계 스크리닝).

    1단계  전국 모든 단지에 자동으로 적용되는 값만 쓴다 — 연식·현재 용적률·
           평균 대지지분. 여기서 나오는 건 "재건축 후보인가"뿐이고,
           추가분담금 같은 숫자는 만들지 않는다.
    2단계  상위 후보 50~100개만 사람이 정비계획·조합 자료를 넣는다.
           그 단지에서만 비례율·권리가액·추가분담금 시나리오가 나온다.

조사하지 않은 단지는 `redev_candidate.manual_status='미조사'` 로 남고,
사업성 함수는 그 단지에 대해 숫자 대신 "확인 불가"를 돌려준다.

특정 단지(부평 동아1단지 등)에 맞춘 하드코딩은 없다. 단지별 값은 전부 DB 에서 온다.
"""

"""숫자 → 이유. 계산된 Calc 를 사람이 읽을 수 있는 문장으로 옮긴다.

계약: **여기서 숫자를 새로 만들지 않는다.** 저장된 필드 외에 새 API 를
호출하지 않는다.

LLM 은 이 계층에만 들어온다(승인된 범위: 리포트 문장 생성). 가격·세금·대출·
거래량·토허·용적률 숫자는 전부 Calc 에서 오고 LLM 은 문장으로 옮기기만 한다.

**아직 비어 있다.** 지금은 결정론적 설명이 그 역할을 한다.

    ranking/explain.py              WHY BUY / WHY NOT / A가 B보다 나은 이유
    ranking/delta_pipeline.detail() 후보 상세 (§48-J)
    ranking/rotation.explain()      순위가 왜 바뀌었나 (§64)

LLM 문장 생성은 위 출력들이 실거래로 검증된 뒤에 붙이는 것이 맞다 —
검증 안 된 숫자를 유창한 문장으로 감싸면 더 위험해진다.
"""

"""수도권 아파트 상대가치 투자 분석 엔진.

기존 토지 급매 탐지기(`collectors/`, `analysis/`, `db/`, `pipeline.py` …)와
**네임스페이스·DB 파일이 모두 분리**돼 있다. 이 패키지의 어떤 코드도 토지 쪽
모듈을 import 하지 않고, `land_invest.db` 를 열지 않는다.

패키지 이름이 계획서의 `apt/` 가 아니라 `apt_engine/` 인 이유:
데비안 계열 리눅스의 python3-apt 가 최상위 `apt` 모듈을 점유해 import 가
상황에 따라 엉뚱한 패키지로 해석될 수 있다. 이름만 바꾸고 구조는 계획서와 같다.

디렉터리별 담당 PHASE는 각 하위 패키지의 __init__.py 참고.
"""

# 계산엔진 버전. 산식이 바뀌면 올린다 — 파생 레코드(calc_trace)에 함께 저장돼,
# "이 숫자가 어느 버전 산식으로 나왔는지"와 재계산 대상 식별에 쓰인다.
ENGINE_VERSION = "0.5.0"
